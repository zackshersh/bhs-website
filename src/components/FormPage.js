import React from 'react';

import "../form.css"

function FormPage({display}) {

    return (
        <div className='Form-Modal'>
            <form>

            </form>
        </div>
    );
}

export default FormPage;