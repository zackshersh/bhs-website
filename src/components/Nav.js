import React from 'react';
import "../nav.css"

function Nav(props) {
    return (
        <nav>
            <button>Services</button>
            <button>Staff</button>
            <button>New Clients</button>
            <button>Contact</button>
        </nav>
    );
}

export default Nav;