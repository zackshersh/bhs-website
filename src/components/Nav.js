import React from 'react';
import "../nav.css"

function Nav(props) {
    return (
        <nav>
            <button>Staff</button>
            <button>Services</button>
        </nav>
    );
}

export default Nav;